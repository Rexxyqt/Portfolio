- [ ] Inspect current theme + Hero subtitle styling
- [ ] Align the sub header color with theme (light blue too bright)
- [ ] Update `src/components/sections/Hero.jsx` (and any shared UI/CSS if needed)
- [ ] Run `npm test`/`npm run build` (or `npm run dev` sanity check)
- [ ] Confirm subtitle appears correctly on dark background

